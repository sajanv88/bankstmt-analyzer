apiVersion: v2
name: bankstmt-analyzer
description: Ingests bank statement PDFs, runs them through Azure OCR and Azure OpenAI, and serves the resulting analysis and chart data.
type: application

# Both are placeholders for local work. CI overrides them from the release
# tag with `helm package --version --app-version`, so the packaged chart
# always names the image it was built alongside.
version: 0.1.0
appVersion: "0.1.0"

home: https://github.com/sajanv88/bankstmt-analyzer
sources:
  - https://github.com/sajanv88/bankstmt-analyzer
keywords:
  - banking
  - ocr
  - analysis
maintainers:
  - name: sajanv88
