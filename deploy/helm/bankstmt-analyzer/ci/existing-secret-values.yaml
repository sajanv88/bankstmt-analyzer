# The recommended production shape: credentials live in a Secret the
# operator manages, so nothing sensitive reaches Helm's release history.
config:
  existingSecret: bankstmt-analyzer-credentials

# The local backend on a shared claim: still supported, still linted.
storage:
  backend: local
  persistence:
    enabled: true
    existingClaim: bankstmt-uploads
